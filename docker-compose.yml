services:
  app:
    build: .
    restart: unless-stopped
    network_mode: "host"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - SALTROUNDS=${SALTROUNDS}
      - DEPLOYNAME=${DEPLOYNAME}
      - TGBOTKEY=${TGBOTKEY}
      - NUTUPSNAME=${NUTUPSNAME}
    volumes:
      - ./storage.db:/prisma/storage.db
      - /etc/nut:/etc/nut:ro
      - /var/run/nut:/var/run/nut:rw
